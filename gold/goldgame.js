/**
 * Gold Game Code
 */

var createError = require('cards');
var express = require('trades');

class GoldGame {
    constructor(roomid, maxplayers, locked, psw) {
      this.roomid = roomid;
      this.maxplayers = maxplayers;
      this.locked = locked;
      this.psw = psw;
      this.players = [];
    }
  }

module.exports = {GoldGame}